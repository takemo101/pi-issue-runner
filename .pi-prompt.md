Implement GitHub Issue #50

## Title
docs: SKILL.mdの前提条件にjqを追加

## Description
## 概要

SKILL.mdの前提条件に`jq`が記載されていない。README.mdには追加済み。

## 現状

**SKILL.md:**
```markdown
## 前提条件

- \`gh\` (GitHub CLI、認証済み)
- \`tmux\`
- \`pi\`
```

**README.md (正しい):**
```markdown
# jq (JSON処理)
which jq
```

## 修正内容

SKILL.mdの前提条件に`jq`を追加：

```markdown
## 前提条件

- \`gh\` (GitHub CLI、認証済み)
- \`tmux\`
- \`pi\`
- \`jq\` (JSON処理)
```

## 優先度
P0 (Critical)

---

## Instructions

You are implementing GitHub Issue #50 in an isolated worktree.

### Step 1: Understand the Issue
- Read the issue description carefully
- If unclear, check related files in the codebase

### Step 2: Implement
- Follow existing code style and patterns
- Keep changes minimal and focused
- Add/update tests if applicable

### Step 3: Verify
- Run existing tests if available
- Check for syntax errors: `bash -n <file>`

### Step 4: Commit & Push
```bash
git add -A
git commit -m "<type>: <description>

Closes #50"
git push -u origin feature/issue-50-docs-skill-md-jq
```

### Step 5: Create & Merge PR
```bash
gh pr create --title "<type>: <short description>" --body "Closes #50"
gh pr merge --merge --delete-branch
```

### Commit Types
- feat: New feature
- fix: Bug fix  
- docs: Documentation
- refactor: Code refactoring
- test: Adding tests
- chore: Maintenance

### On Error
- If tests fail, fix the issue before committing
- If PR merge fails, report the error
