Implement GitHub Issue #51

## Title
docs: ディレクトリ構造にlib/log.shを追加

## Description
## 概要

AGENTS.mdとREADME.mdのディレクトリ構造に`lib/log.sh`が記載されていない。

## 現状

**ドキュメントの記載:**
```
├── lib/
│   ├── config.sh
│   ├── github.sh
│   ├── worktree.sh
│   └── tmux.sh
```

**実際のファイル:**
```bash
$ ls lib/
config.sh  github.sh  log.sh  tmux.sh  worktree.sh
```

## 修正内容

AGENTS.mdとREADME.mdのディレクトリ構造を更新：

```
├── lib/
│   ├── config.sh
│   ├── github.sh
│   ├── log.sh           # 追加
│   ├── tmux.sh
│   └── worktree.sh
```

## 優先度
P0 (Critical)

---

## Instructions

You are implementing GitHub Issue #51 in an isolated worktree.

### Step 1: Understand the Issue
- Read the issue description carefully
- If unclear, check related files in the codebase

### Step 2: Implement
- Follow existing code style and patterns
- Keep changes minimal and focused
- Add/update tests if applicable

### Step 3: Verify
- Run existing tests if available
- Check for syntax errors: `bash -n <file>`

### Step 4: Commit & Push
```bash
git add -A
git commit -m "<type>: <description>

Closes #51"
git push -u origin feature/issue-51-docs-lib-log-sh
```

### Step 5: Create & Merge PR
```bash
gh pr create --title "<type>: <short description>" --body "Closes #51"
gh pr merge --merge --delete-branch
```

### Commit Types
- feat: New feature
- fix: Bug fix  
- docs: Documentation
- refactor: Code refactoring
- test: Adding tests
- chore: Maintenance

### On Error
- If tests fail, fix the issue before committing
- If PR merge fails, report the error
