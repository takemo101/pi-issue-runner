# 実装計画: Issue #242

## 概要

README.mdに記載されているテストディレクトリ構造が実際のプロジェクト構造と異なっているため、ドキュメントを実態に合わせて修正する。

## 問題の詳細

### 現在のREADME.mdの記述（誤り）

```
test/
├── helpers/                # テストヘルパー    ← 存在しない
│   └── mocks.sh            # モック関数        ← 存在しない
```

### 実際のディレクトリ構造

```
test/
├── lib/                    # ライブラリのユニットテスト
├── scripts/                # スクリプトの統合テスト
├── regression/             # 回帰テスト ← README.mdに記載なし
├── fixtures/               # テスト用フィクスチャ
│   └── sample-config.yaml
└── test_helper.bash        # Bats共通ヘルパー（モック関数含む）
```

## 影響範囲

- `README.md` のテスト構造セクション（約355-369行目）

## 実装ステップ

1. README.mdのテスト構造セクションを特定
2. `helpers/` ディレクトリと `mocks.sh` の記述を削除
3. `regression/` ディレクトリの記述を追加
4. `test_helper.bash` の説明を「モック関数含む」に更新
5. 変更をコミット

## テスト方針

- ドキュメント変更のみのため、自動テストは不要
- 手動で実際のディレクトリ構造と一致することを確認

## リスクと対策

- **リスク**: 特になし（ドキュメント修正のみ）
- **対策**: 変更前後でディレクトリ構造を再確認

## 見積もり

作業時間: 15分
