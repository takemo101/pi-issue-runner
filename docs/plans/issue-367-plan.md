# Issue #367 実装計画

## 概要

`install.sh` の20行目で宣言されている未使用変数 `OPTIONAL_DEPS=""` を削除し、ShellCheck SC2034 警告を解消する。

## 影響範囲

- **対象ファイル**: `install.sh`
- **変更行**: 20行目（`OPTIONAL_DEPS=""` を削除）
- **影響**: なし（変数は未使用のため、削除しても動作に影響しない）

## 実装ステップ

1. `install.sh` の20行目（`OPTIONAL_DEPS=""` とそのコメント行）を削除
2. ShellCheck で警告が解消されたことを確認
3. `./install.sh --help` で正常動作を確認
4. コミット

## テスト方針

1. **静的解析**: `shellcheck -x install.sh` がクリーンに通ること
2. **機能テスト**: `./install.sh --help` が正常に動作すること
3. **回帰テスト**: 既存のインストール機能が正常に動作すること

## リスクと対策

| リスク | 対策 |
|--------|------|
| なし | 変数は完全に未使用のため、削除による副作用はない |

## 見積もり

約5分
