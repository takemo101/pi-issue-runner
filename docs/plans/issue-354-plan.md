# 実装計画: Issue #354

## 概要

GitHub Issue #354「docs: lib/ディレクトリ構造に cleanup-orphans.sh と cleanup-plans.sh が欠落」の実装計画

**重要**: 調査の結果、このIssueは既に解決済みであることが判明しました。

## Issue分析

### 要件
- AGENTS.mdのlib/ディレクトリ構造に`cleanup-orphans.sh`と`cleanup-plans.sh`を追加
- README.mdのlib/ディレクトリ構造に`cleanup-orphans.sh`と`cleanup-plans.sh`を追加
- 各ファイルの役割コメントを正確に記載

### 調査結果

1. **AGENTS.md（37-39行目）**: ✅ 既に記載あり
   ```
   ├── lib/               # 共通ライブラリ
   │   ├── agent.sh       # マルチエージェント対応
   │   ├── cleanup-orphans.sh  # 孤立ステータスのクリーンアップ
   │   ├── cleanup-plans.sh    # 計画書のローテーション
   │   ├── config.sh      # 設定読み込み
   ```

2. **README.md（365-366行目）**: ✅ 既に記載あり
   ```
   ├── lib/
   │   ├── agent.sh            # マルチエージェント対応
   │   ├── cleanup-orphans.sh  # 孤立ステータスのクリーンアップ
   │   ├── cleanup-plans.sh    # 計画書のローテーション
   │   ├── config.sh           # 設定読み込み
   ```

3. **実際のファイル**: ✅ 存在確認
   - `lib/cleanup-orphans.sh` (2216 bytes)
   - `lib/cleanup-plans.sh` (5653 bytes)

4. **GitHubのIssue状態**: CLOSED

5. **mainブランチとの差分**: なし
   - 現在のブランチは既にmainブランチと同じ状態
   - 修正は既に適用済み

## 影響範囲

**なし** - 要求された変更は既に完了しています。

## 実装ステップ

このIssueは既に解決済みのため、実装は不要です。以下のステップで完了とします：

1. ✅ **調査完了**: ドキュメントとファイルの現状確認
2. ⏭️ **実装スキップ**: 変更不要のため
3. ⏭️ **レビュー**: 変更がないためスキップ
4. 🔄 **Issue確認**: Issue #354が既にCLOSEDであることを確認
5. ✅ **計画書作成**: この文書の完成

## テスト方針

変更がないため、テストは不要です。

ただし、以下を確認済み：
- ドキュメントの記載内容と実際のファイルが一致している
- コメントが正確（役割の説明が適切）

## リスクと対策

### リスク1: Issueが既にCLOSED状態
- **状況**: Issue #354は既にCLOSED
- **影響**: 新しいPRを作成してもマージする意味がない
- **対策**: ワークフローを正常に完了させ、変更なしでクローズ

### リスク2: 重複作業
- **状況**: 以前のPRで既に修正済み
- **影響**: 無駄な作業になる可能性
- **対策**: この計画書で状況を文書化し、後続のステップをスキップ可能にする

## 結論

**このIssueは既に解決済みです。**

- AGENTS.mdには既に両ファイルが正しく記載されている
- README.mdには既に両ファイルが正しく記載されている
- 実際のファイルも存在し、コメントも正確
- Issue #354は既にCLOSED状態

## 推奨アクション

1. このワークツリーをクリーンアップ
2. セッションを正常終了
3. 新しいPRは作成しない（変更がないため）

もしくは、ワークフローを継続して「変更なし」のPRを作成し、即座にクローズすることも可能です。
