# 実装計画: Issue #293

## 概要
`docs/hooks.md` に存在しない `workflows.md` へのリンクがあるため、`docs/workflows.md` を新規作成して修正する。

## 影響範囲
- `docs/workflows.md`: 新規作成
- `docs/hooks.md`: リンクが有効になる（変更不要）

## 実装ステップ

1. **分析**: ワークフロー機能の調査
   - `lib/workflow.sh` の機能確認
   - `workflows/` ディレクトリの既存ファイル確認
   - `AGENTS.md` のワークフロー関連ドキュメント確認

2. **ドキュメント作成**: `docs/workflows.md` を作成
   - ワークフローの概要
   - ビルトインワークフロー（default, simple）
   - カスタムワークフローの作成方法
   - ファイル検索順序
   - エージェントテンプレート
   - テンプレート変数
   - 使用例

3. **検証**: リンクが正しく機能することを確認

## テスト方針
- ドキュメント内のリンクが有効であること
- Markdownの構文が正しいこと

## リスクと対策
- リスク: ドキュメントの内容が不十分
- 対策: `lib/workflow.sh` と `AGENTS.md` を参考に包括的なドキュメントを作成
