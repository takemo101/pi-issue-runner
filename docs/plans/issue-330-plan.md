# Issue #330 実装計画書

## 概要

孤立したステータスファイル（`.worktrees/.status/`）のクリーンアップを実行し、READMEの「メンテナンス」セクションが適切に文書化されていることを確認する。

## 現状分析

- `.worktrees/.status/` に約50個のステータスファイルが存在
- `--dry-run` で確認した結果、44個が孤立ファイル
- `cleanup.sh --orphans` 機能は既に実装済み
- READMEにメンテナンスセクションは既に存在

## 影響範囲

- `.worktrees/.status/` ディレクトリ内のJSONファイル
- 実行中のセッションには影響なし

## 実装ステップ

1. **孤立ファイルの削除**
   - `./scripts/cleanup.sh --orphans` を実行
   - 削除されたファイル数を確認

2. **ドキュメント確認**
   - README.mdの「メンテナンス」セクションを確認
   - 既に適切に文書化されていることを確認

3. **テスト**
   - 孤立ファイルが削除されたことを確認
   - `list.sh` の出力が正常であることを確認

## テスト方針

- クリーンアップ後に `ls .worktrees/.status/` で残りファイル数を確認
- 現在実行中のセッション（issue-330-chore）のステータスファイルが残っていることを確認

## リスクと対策

| リスク | 対策 |
|--------|------|
| 実行中セッションのステータスが削除される | クリーンアップスクリプトは孤立ファイルのみを対象とする |
| 不要なファイルが残る | `--dry-run` で事前確認済み |

## 完了条件

- [x] 孤立したステータスファイルが削除された
- [x] READMEにメンテナンス手順が文書化されている
