# 実装計画: Issue #185 - LOG_LEVEL 環境変数のドキュメント追加

## 概要

`lib/log.sh` で実装されている `LOG_LEVEL` 環境変数によるログレベル制御機能を `docs/configuration.md` にドキュメント化する。

## 影響範囲

- **変更対象**: `docs/configuration.md`
- **関連コード**: `lib/log.sh`（参照のみ、変更なし）

## 実装ステップ

1. `docs/configuration.md` の「環境変数」テーブルに `LOG_LEVEL` を追加
2. ログレベルの詳細セクションを追加（利用可能なレベル、優先順位）
3. 使用例を追加

## 詳細な変更内容

### 1. 環境変数テーブルへの追加

| 環境変数 | 説明 | 例 |
|---------|------|-----|
| `LOG_LEVEL` | ログレベル（DEBUG, INFO, WARN, ERROR, QUIET） | `DEBUG` |

### 2. 新しいセクション追加

「環境変数」セクションの後に「ログレベル」セクションを追加：

- ログレベルの一覧と説明
- 優先順位（DEBUG < INFO < WARN < ERROR < QUIET）
- 使用例

## テスト方針

- ドキュメントの変更のみのため、コードテストは不要
- マークダウンの構文が正しいことを確認

## リスクと対策

- **リスク**: なし（ドキュメントのみの変更）
- **対策**: 既存のドキュメントスタイルに合わせる

## 受け入れ条件

- [x] `docs/configuration.md` に LOG_LEVEL が記載された
- [x] 利用可能なログレベルが明記された
- [x] 使用例が記載された
