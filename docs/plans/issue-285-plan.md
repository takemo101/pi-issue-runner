# Issue #285 実装計画

## 概要

`docs/hooks.md` の「関連ドキュメント」セクションにある `./workflows.md` へのリンクが存在しないファイルを参照している問題を修正する。

## 問題の分析

- **場所**: `docs/hooks.md:155`
- **問題のリンク**: `[ワークフロー](./workflows.md)`
- **原因**: `docs/workflows.md` が存在しない

## 影響範囲

| ファイル | 変更内容 |
|----------|----------|
| `docs/workflows.md` | 新規作成 |
| `docs/hooks.md` | 変更なし（リンク先を作成することで自動解決） |

## 実装ステップ

### 1. `docs/workflows.md` の作成

`workflows/` ディレクトリに `default.yaml` と `simple.yaml` が存在するため、これらの説明を含むドキュメントを作成する。

ドキュメントに含める内容：
- ワークフローの概要
- ビルトインワークフロー（default, simple）の説明
- カスタムワークフローの作成方法
- ワークフロー検索順序
- エージェントテンプレートとの関連

### 2. リンクの確認

- `docs/hooks.md` のリンクが正しく機能することを確認
- 他のドキュメントでリンク切れがないか確認

## テスト方針

1. `docs/workflows.md` が存在することを確認
2. リンク先のパスが正しいことを確認
3. ドキュメント内のリンクが全て有効であることを確認

## リスクと対策

| リスク | 対策 |
|--------|------|
| 既存ドキュメントとの整合性 | SPECIFICATION.md と configuration.md の内容と整合性を取る |
| 情報の重複 | 必要最小限の説明にとどめ、詳細は関連ドキュメントへ参照 |

## 完了条件

- [x] `docs/workflows.md` を作成
- [x] `docs/hooks.md` のリンクが有効になる
- [x] すべてのリンクが有効なファイルを参照している
