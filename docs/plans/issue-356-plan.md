# Issue #356 実装計画

## 概要

README.md の「前提条件」セクションで `yq` を必須ツールとして記載していますが、実際は `lib/yaml.sh` でフォールバック対応されているためオプションです。この不整合を修正します。

## 影響範囲

- **README.md**: 前提条件セクションの `yq` の記載を修正

## 実装ステップ

1. README.md の前提条件セクションで yq がオプションであることを明記
2. AGENTS.md との一貫性を確認

## 変更内容

### 現在の記載（README.md 39-41行目付近）

```bash
# yq (YAML処理)
which yq
```

### 修正後

```bash
# yq (YAML処理、オプション - なくてもフォールバックで動作)
which yq  # オプション
```

## テスト方針

- 変更はドキュメントのみのため、特別なテストは不要
- ShellCheck はシェルスクリプトのみ対象なので影響なし

## リスクと対策

- **リスク**: なし（ドキュメント修正のみ）
- **対策**: N/A

## 見積もり

約10分
