# Implementation Plan: Issue #284

## 概要

AGENTS.md のディレクトリ構造セクションを最新化し、実際に存在するファイルを正しく記載する。

## 影響範囲

- `AGENTS.md` - ドキュメントのみの変更

## 調査結果

### 実際のファイル構成と AGENTS.md の差分

| セクション | ファイル | 実ファイル | AGENTS.md記載 | 対応 |
|------------|----------|------------|---------------|------|
| lib/ | hooks.sh | ✅ 存在 | ❌ 未記載 | 追加必要 |
| test/lib/ | github.bats | ✅ 存在 | ✅ 記載済み | 対応不要 |
| test/lib/ | hooks.bats | ✅ 存在 | ❌ 未記載 | 追加必要 |

**注意**: Issue では `github.bats` が未記載と報告されていたが、調査の結果、既に記載されていた。

## 実装ステップ

1. AGENTS.md の `lib/` セクションに `hooks.sh` を追加（github.sh と log.sh の間）
2. AGENTS.md の `test/lib/` セクションに `hooks.bats` を追加（github.bats と log.bats の間）
3. 変更をコミット

## テスト方針

- 実際のディレクトリ構造とドキュメントの整合性を目視確認
- ドキュメント変更のため、追加テスト不要

## リスクと対策

- リスク: ファイルの並び順の不一致
- 対策: アルファベット順を維持して追加
