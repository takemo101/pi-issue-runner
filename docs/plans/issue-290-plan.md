# Issue #290 実装計画

## 概要

README.mdの「テスト構造」セクションに `test/lib/hooks.bats` が記載されていない問題を修正する。

## 影響範囲

- `README.md` のテスト構造セクションのみ

## 実装ステップ

1. README.mdのtest/lib/セクションを確認
2. `hooks.bats` をアルファベット順（`github.bats` と `log.bats` の間）に追加
3. コミットしてPR作成

## テスト方針

- ドキュメント変更のみのため、Batsテストは不要
- 視覚的にアルファベット順であることを確認

## リスクと対策

- 低リスク: ドキュメント変更のみ
- 対策: 既存の記載形式に合わせる
