# Issue #541 実装計画

## 概要

README.mdのコマンド表に`pi-batch`が2回記載されている問題を修正します。

- 1回目（57行目）: `pi-batch` | 複数Issue一括実行
- 2回目（68行目）: `pi-batch` | 複数Issueを依存関係順にバッチ実行

## 影響範囲

- `README.md` のみ

## 実装ステップ

1. 57行目の説明文を「複数Issueを依存関係順にバッチ実行」に変更
2. 68行目の重複エントリを削除

## テスト方針

- `grep -n "pi-batch" README.md` で1回のみ出現することを確認

## リスクと対策

- リスク: なし（ドキュメント修正のみ）

## 完了条件

- [x] `pi-batch` がコマンド表に1回のみ記載されている
- [x] 説明文が「複数Issueを依存関係順にバッチ実行」に統一されている
