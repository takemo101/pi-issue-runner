# Issue #297 実装計画書

## 概要

AGENTS.md のディレクトリ構造セクションに `install.sh` と `uninstall.sh` の記載を追加する。

## 影響範囲

- **AGENTS.md** - ドキュメントのディレクトリ構造セクション

## 実装ステップ

1. AGENTS.md のディレクトリ構造セクションを確認
2. `README.md` の後に `install.sh` と `uninstall.sh` を追加
3. コメントを日本語で追加（既存のスタイルに合わせる）

## 変更内容

現在:
```
├── README.md          # プロジェクト説明
├── scripts/           # 実行スクリプト
```

変更後:
```
├── README.md          # プロジェクト説明
├── install.sh         # グローバルインストール
├── uninstall.sh       # アンインストール
├── scripts/           # 実行スクリプト
```

## テスト方針

- ドキュメント変更のみのため、自動テストは不要
- 視覚的にフォーマットが正しいことを確認

## リスクと対策

- 低リスク: ドキュメントの軽微な変更のみ
