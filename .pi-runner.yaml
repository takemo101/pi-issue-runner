# pi-issue-runner 設定ファイル
# 詳細: https://github.com/takemo101/pi-issue-runner/blob/main/docs/configuration.md

# =====================================
# Worktree設定
# =====================================
worktree:
  # worktreeの作成先ディレクトリ
  base_dir: ".worktrees"

  # デフォルトのベースブランチ（--base オプションで上書き可能）
  base_branch: "origin/main"

  # worktree作成時にコピーするファイル
  copy_files:
    - .env
    - .env.local
    - .envrc

  # worktree作成時にコピーするディレクトリ（再帰コピー）
  copy_dirs:
    - .opencode

# =====================================
# マルチプレクサ設定
# =====================================
multiplexer:
  # 使用するマルチプレクサ: tmux | zellij
  type: "tmux"

  # セッション名のプレフィックス（例: pi-issue-42）
  session_prefix: "pi"

  # セッション内で起動するか
  start_in_session: true

# =====================================
# エージェント設定
# =====================================
agent:
  # エージェントタイプ: pi | claude | opencode | custom
  # 空の場合は pi セクションの設定を使用（後方互換性）
  type: "pi"

  # カスタムコマンド（type: custom の場合に使用）
  # command: "my-agent"

  # カスタムテンプレート（type: custom の場合に使用）
  # 利用可能な変数: {{command}}, {{args}}, {{prompt_file}}
  # template: "{{command}} {{args}} < {{prompt_file}}"

  # エージェントに渡す追加引数
  args:
    - --provider
    - anthropic
    - --model
    - claude-opus-4-6

# =====================================
# GitHub設定
# =====================================
github:
  # Issueのコメントをプロンプトに含める
  include_comments: true

  # 最大コメント数（0 = 無制限）
  max_comments: 10

# =====================================
# 並列実行設定
# =====================================
parallel:
  # 同時実行数の上限（0 = 無制限）
  max_concurrent: 5

# =====================================
# auto ワークフロー選択設定
# =====================================
auto:
  # auto選択用のAIプロバイダー（省略時: agent設定から推定 or anthropic）
  provider: anthropic

  # auto選択用のモデル（軽量・高速なモデル推奨）
  # 省略時: claude-haiku-4-5
  model: claude-haiku-4-5

# =====================================
# 複数ワークフロー定義
# -w 省略時は auto（AIがIssue内容から自動選択）
# =====================================
workflows:
  # 標準: 計画→実装→品質チェック→レビュー→マージ
  default:
    description: 標準ワークフロー（計画・実装・品質チェック・レビュー・マージ）
    steps:
      - plan
      - implement
      - test
      - run: "shellcheck -x scripts/*.sh lib/*.sh"
        description: "ShellCheck 静的解析"
      - run: "bats --jobs 2 test/"
        timeout: 900
        description: "Bats テスト"
      - review
      - merge

  # 機能追加: 計画→実装→品質チェック→観点別レビュー→マージ
  feature:
    description: 新機能・大規模変更（lib/scripts への新関数追加、新スクリプト作成）
    steps:
      - plan
      - implement
      - test
      - run: "shellcheck -x scripts/*.sh lib/*.sh"
        description: "ShellCheck 静的解析"
      - run: "bats --jobs 2 test/"
        timeout: 900
        description: "Bats テスト"
      - call: review-bugs
        timeout: 900
        description: "バグ・ロジックエラーレビュー"
      - call: review-security
        timeout: 900
        description: "セキュリティレビュー"
      - call: review-ai-antipattern
        timeout: 900
        description: "AI実装アンチパターンレビュー"
      - merge
    agent:
      type: opencode
      args:
        - -m
        - anthropic/claude-opus-4-6
    context: |
      ## 技術スタック
      - Bash 4.0+ / ShellCheck 準拠
      - テスト: Bats (Bash Automated Testing System)
      - YAML: yq + フォールバック簡易パーサー (lib/yaml.sh)

      ## 方針
      - 全ファイルに `set -euo pipefail` を設定
      - 関数は小文字スネークケース、ローカル変数は `local` 宣言
      - 新しい lib/ には対応する test/lib/*.bats を必ず作成
      - 新しい scripts/ には対応する test/scripts/*.bats を必ず作成
      - AGENTS.md のディレクトリ構造を更新すること

  # バグ修正・リファクタリング: 実装→品質チェック→バグ+セキュリティレビュー→マージ
  fix:
    description: バグ修正・リファクタリング・セキュリティ修正
    steps:
      - implement
      - test
      - run: "shellcheck -x scripts/*.sh lib/*.sh"
        description: "ShellCheck 静的解析"
      - run: "bats --jobs 2 test/"
        timeout: 900
        description: "Bats テスト"
      - call: review-bugs
        timeout: 900
        description: "バグ・ロジックエラーレビュー"
      - call: review-security
        timeout: 900
        description: "セキュリティレビュー"
      - merge
    context: |
      ## 方針
      - 回帰テスト (test/regression/) の追加を検討すること
      - 修正範囲を最小限に抑える
      - ShellCheck 警告を解消すること
      - 既存テストが壊れないことを確認

  # ドキュメント: 実装→マージ（軽量モデルで十分、品質チェック不要）
  docs:
    description: ドキュメント更新（README, AGENTS.md, docs/ 以下）
    steps:
      - implement
      - merge
    agent:
      type: pi
      args:
        - --provider
        - anthropic
        - --model
        - claude-sonnet-4-5
    context: |
      ## 方針
      - docs/ 以下の既存ドキュメントとの整合性を確認
      - AGENTS.md のディレクトリ構造が実態と一致しているか確認
      - コード例は実際に動作するものを記載

  # テスト追加: 実装→品質チェック→レビュー→マージ
  test:
    description: テスト追加・テスト改善
    steps:
      - implement
      - test
      - run: "shellcheck -x scripts/*.sh lib/*.sh"
        description: "ShellCheck 静的解析"
      - run: "bats --jobs 2 test/"
        timeout: 900
        description: "Bats テスト"
      - review
      - merge
    context: |
      ## 方針
      - Bats テストの規約に従う（test_helper.bash のモック関数を活用）
      - setup/teardown で環境を確実にクリーンアップ
      - 並列実行 (BATS_JOBS=4) でも安定するよう、グローバル状態を汚染しない
      - reset_yaml_cache, _YQ_CHECK_RESULT="" を setup で呼ぶ

  # 観点別レビュー用ワークフロー（call: ステップから呼び出される）
  review-bugs:
    description: バグ・ロジックエラーのレビュー
    steps:
      - review-bugs
    agent:
      type: pi
      args: [--provider, anthropic, --model, claude-haiku-4-5]

  review-security:
    description: セキュリティレビュー
    steps:
      - review-security
    agent:
      type: pi
      args: [--provider, anthropic, --model, claude-haiku-4-5]

  review-architecture:
    description: 設計・構造レビュー
    steps:
      - review-architecture
    agent:
      type: pi
      args: [--provider, anthropic, --model, claude-haiku-4-5]

  review-ai-antipattern:
    description: AI実装アンチパターンレビュー
    steps:
      - review-ai-antipattern
    agent:
      type: pi
      args: [--provider, anthropic, --model, claude-haiku-4-5]

  # 簡易修正: 実装→マージのみ（軽量モデルで高速・低コスト、品質チェック不要）
  quickfix:
    description: typo修正・設定値変更・コメント修正など軽微な変更
    steps:
      - implement
      - merge
    agent:
      type: pi
      args:
        - --provider
        - anthropic
        - --model
        - claude-sonnet-4-5

# =====================================
# 計画書設定
# =====================================
plans:
  # 保持する計画書の件数（0 = 全て保持）
  keep_recent: 10

  # 計画書ディレクトリ
  dir: "docs/plans"

# =====================================
# improve 設定
# =====================================
improve:
  # カスタムレビュープロンプトファイル
  # 検索順: この設定 → agents/improve-review.md → .pi/agents/improve-review.md → ビルトイン
  # テンプレート変数: {{max_issues}}, {{session_label}}, {{review_context}}
  review_prompt_file: "agents/improve-review.md"

  # ログ設定
  logs:
    # 保持するログファイルの件数（0 = 全て保持）
    keep_recent: 10

    # 保持するログファイルの日数（0 = 日数制限なし）
    keep_days: 7

    # ログファイルの保存ディレクトリ
    dir: ".improve-logs"

# =====================================
# Hook設定
# =====================================
hooks:
  # インラインコマンドの実行を許可（デフォルト: false）
  # .pi-runner.yaml にインラインhookを書く場合は true に設定
  allow_inline: true

  # 成功時の通知
  on_success: |
    osascript -e 'display notification "Issue #'"$PI_ISSUE_NUMBER"' が完了しました" with title "Pi Issue Runner" subtitle "✅ 完了" sound name "Glass"'

  # エラー時の通知
  on_error: |
    osascript -e 'display notification "'"$PI_ERROR_MESSAGE"'" with title "Pi Issue Runner" subtitle "❌ Issue #'"$PI_ISSUE_NUMBER"' エラー" sound name "Basso"'

  # セッション開始時（コメント解除で有効化）
  on_start: |
    osascript -e 'display notification "Issue #'"$PI_ISSUE_NUMBER"' を開始しました" with title "Pi Issue Runner" subtitle "🚀 開始" sound name "Pop"'

  # クリーンアップ後（コメント解除で有効化）
  on_cleanup: |
    echo "Cleanup completed for Issue #$PI_ISSUE_NUMBER"

  # improve開始時
  on_improve_start: |
    osascript -e 'display notification "改善ループを開始します（最大 '"$PI_MAX_ITERATIONS"' 回）" with title "Pi Issue Runner" subtitle "🔄 improve開始" sound name "Pop"'

  # improve終了時
  on_improve_end: |
    osascript -e 'display notification "改善ループ完了: 作成 '"$PI_ISSUES_CREATED"' / 成功 '"$PI_ISSUES_SUCCEEDED"' / 失敗 '"$PI_ISSUES_FAILED"'" with title "Pi Issue Runner" subtitle "🏁 improve終了" sound name "Glass"'

  # イテレーション開始時
  on_iteration_start: |
    osascript -e 'display notification "イテレーション '"$PI_ITERATION"'/'"$PI_MAX_ITERATIONS"' を開始" with title "Pi Issue Runner" subtitle "🔁 イテレーション開始" sound name "Pop"'

  # イテレーション終了時
  on_iteration_end: |
    osascript -e 'display notification "イテレーション '"$PI_ITERATION"'/'"$PI_MAX_ITERATIONS"' 完了: 成功 '"$PI_ISSUES_SUCCEEDED"' / 失敗 '"$PI_ISSUES_FAILED"'" with title "Pi Issue Runner" subtitle "✅ イテレーション終了" sound name "Purr"'

  # レビュー完了時
  on_review_complete: |
    osascript -e 'display notification "レビュー完了: '"$PI_REVIEW_ISSUES_COUNT"' 件の問題を発見" with title "Pi Issue Runner" subtitle "🔍 レビュー完了" sound name "Tink"'

# =====================================
# Watcher設定
# =====================================
watcher:
  # PRマージタイムアウト時に強制クリーンアップ
  # COMPLETEマーカー検出後、PRがマージされなくてもセッションを終了する
  force_cleanup_on_timeout: true

  # エラー検知時にターミナルを自動で開いてセッションにアタッチする
  # Terminal.app以外（Ghostty等）を使用する場合はfalseに設定
  auto_attach: false
